import React, { useEffect, useState } from 'react';

/** Renders a Bookmarks page. 
 * 
 * -Displays saved "bookmarks" for logged in user.
 * 
*/

const Bookmarks = () => {
	return (
		<div>
			<h1>Bookmarks</h1>
		</div>
	);
};

export default Bookmarks;
